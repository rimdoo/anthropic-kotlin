# Golden-set evaluation — 2026-05-13T07:09:13Z

SDK rev: uncommitted (initial M1+M2+M3)
Scope: case 1 only (`messages-create-basic`) — first pipeline cycle.

## Summary

- Cases evaluated: 1
- **Pass: 1**
- Fail: 0
- Manual-review: 0

## Table

| id | compile | criteria pass/total | overall | notes |
| --- | --- | --- | --- | --- |
| messages-create-basic | ✅ | 4/4 | ✅ pass | — |

## Detail — `messages-create-basic`

### Compile
`./gradlew :golden-runner:compileKotlin` → BUILD SUCCESSFUL (954ms)

Source: `golden-runner/src/main/kotlin/cases/Case_messages_create_basic.kt` (auto-generated from YAML).

### Static criteria

| id | check | command | result |
| --- | --- | --- | --- |
| `suspend-create` | grep_must_match `suspend fun.*AnthropicClient\.createMessage` | `grep -rE "suspend fun.*AnthropicClient\.createMessage" sdk/src/main/kotlin` | ✅ matched `Messages.kt:suspend fun AnthropicClient.createMessage(` |
| `no-completable-future-leak` | grep_must_not_match `CompletableFuture` | `grep -rE "CompletableFuture" sdk/src/main/kotlin` | ✅ 0 matches |
| `no-optional-leak` | grep_must_not_match `:\s*Optional<` | `grep -rE ":\s*Optional<" sdk/src/main/kotlin` | ✅ 0 matches |
| `response-text-convenience` | grep_must_match `val MessageResponse\.text` | `grep -rE "val MessageResponse\.text" sdk/src/main/kotlin` | ✅ matched `MessageResponse.kt:val MessageResponse.text: String` |

## Files produced this cycle

- `settings.gradle.kts`
- `build.gradle.kts`
- `gradle.properties`
- `gradle/libs.versions.toml`
- `gradle/wrapper/gradle-wrapper.{jar,properties}` (8.10)
- `gradlew`, `gradlew.bat`
- `sdk/build.gradle.kts`
- `sdk/src/main/kotlin/com/sendbird/anthropic/Model.kt` (typealias)
- `sdk/src/main/kotlin/com/sendbird/anthropic/Message.kt`
- `sdk/src/main/kotlin/com/sendbird/anthropic/MessageResponse.kt`
- `sdk/src/main/kotlin/com/sendbird/anthropic/Messages.kt`
- `golden-runner/build.gradle.kts`
- `golden-runner/src/main/kotlin/cases/Case_messages_create_basic.kt`

## Top notes for next cycle

1. **Evaluator's grep regex compatibility** — case YAMLs use `^(?!.*private).*` negative lookahead, which `grep -E` (POSIX ERE) does not support. The current cycle worked because there were 0 matches for the inner pattern anyway, but for future cases the evaluator should use `grep -P` (PCRE) or rewrite the patterns to avoid lookahead. **Action**: harden evaluator before case 2.
2. **`Model.CLAUDE_OPUS_4_7` access** — confirmed accessible from Kotlin via Java SDK's static field on the typealiased class. No wrapping needed.
3. **`@file:JvmName("AnthropicKt")`** in `Messages.kt` is in place. When case 4 adds `streamMessage` it should land in the same package and either inherit the same `JvmName` (separate file with the same `@file:JvmName("AnthropicKt")` annotation) or be co-located in `Messages.kt`. Decide before M-for-case-4.
4. **Next case candidate** — `messages-multi-turn` (case 2) is the cheapest next step. Requires only adding `Message.assistant(String)` factory.
